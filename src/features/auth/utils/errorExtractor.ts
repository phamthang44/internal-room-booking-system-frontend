/**
 * @deprecated Import from `@shared/errors/extractApiErrorMessage` instead.
 * Re-exports preserve existing feature imports.
 */
export {
  extractApiErrorMessage as extractErrorMessage,
  getApiErrorCode as getErrorCode,
  isValidationApiError as isValidationError,
} from "@shared/errors/extractApiErrorMessage";
