# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
documents
.gitnexus

.claude
.cursor
.github
.shared
 
# Batch helper files 
*.bat 

CLAUDE.md
AGENTS.md

.agents

# keep graph outputs, skip heavy/local-only files

# optional: commit for shared extraction speed, skip to keep repo small
graphify-out/cache/

# mtime-based, invalid after git clone - always gitignore this
graphify-out/manifest.json

# local token tracking, not useful to share
graphify-out/cost.json

# graphify scratch files (intermediate pipeline state, never commit)
.graphify_*.json
.graphify_python

# accidental nested graphify outputs inside feature folders
src/**/graphify-out/